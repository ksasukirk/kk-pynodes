# 节点组库

所有节点组统一使用JSON格式存储。

## 目录说明

- `builtin/` - 系统内置的节点组（随应用提供）
- `user/` - 用户创建的节点组（通过UI创建）

## JSON格式

所有节点组使用统一的JSON格式，包含：
- 节点组信息（名称、描述）
- 内部节点列表
- 节点间连接
- 组输入输出映射

## 使用方式

1. **创建节点组**：在编辑器中选择节点，按 Ctrl+G
2. **使用节点组**：从节点组库面板拖拽或双击
3. **编辑节点组**：选中节点组，按 Tab 键进入内部编辑
4. **导出分享**：右键节点组，选择"导出"

